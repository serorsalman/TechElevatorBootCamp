package com.techelevator;

import java.math.BigDecimal;

public class Bluray extends Movies {
    public Bluray(String title, String format, BigDecimal rentalPrice) {
        super(title, format, rentalPrice);
    }
}
