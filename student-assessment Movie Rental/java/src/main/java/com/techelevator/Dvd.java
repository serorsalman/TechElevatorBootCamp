package com.techelevator;

import java.math.BigDecimal;

public class Dvd extends Movies {
    public Dvd(String title, String format, BigDecimal rentalPrice) {
        super(title, format, rentalPrice);
    }
}
