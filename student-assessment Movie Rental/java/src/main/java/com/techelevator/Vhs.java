package com.techelevator;

import java.math.BigDecimal;

public class Vhs extends Movies {
    public Vhs(String title, String format, BigDecimal rentalPrice) {
        super(title, format, rentalPrice);
    }
}